<form method="POST" action="<?= htmlspecialchars($action ?? '', ENT_QUOTES, 'UTF-8') ?>" class="flex flex-col gap-6 max-w-xl mx-auto p-8 bg-white rounded-lg shadow-lg">
    <div class="flex flex-col gap-2">
        <label for="titulo" class="font-semibold text-[#172b4d] text-[0.9rem]">Título</label>
        <input type="text" id="titulo" name="titulo"
            value="<?= htmlspecialchars($task['titulo'] ?? '', ENT_QUOTES, 'UTF-8') ?>"
            class="p-3 bg-[#ebecf0] border-none rounded-lg text-[0.8rem] shadow focus:outline-none focus:ring-2 focus:ring-[#0052cc]"
            maxlength="200" <?= !empty($readonly) ? 'readonly' : '' ?> required>
    </div>
    <div class="flex flex-col gap-2">
        <label for="descripcion" class="font-semibold text-[#172b4d] text-[0.9rem]">Descripción</label>
        <textarea id="descripcion" name="descripcion" rows="4"
            class="p-3 bg-[#ebecf0] border-none rounded-lg text-[0.8rem] shadow focus:outline-none focus:ring-2 focus:ring-[#0052cc] resize-y min-h-[100px]"
            maxlength="1000" <?= !empty($readonly) ? 'readonly' : '' ?>><?= htmlspecialchars($task['descripcion'] ?? '', ENT_QUOTES, 'UTF-8') ?></textarea>
    </div>
    <div class="flex flex-col gap-2">
        <label for="estado" class="font-semibold text-[#172b4d] text-[0.9rem]">Estado</label>
        <select id="estado" name="estado"
            class="p-3 bg-[#ebecf0] border-none rounded-lg text-[0.8rem] shadow focus:outline-none focus:ring-2 focus:ring-[#0052cc]"
            <?= !empty($readonly) ? 'disabled' : '' ?> required>
            <option value="pendiente" <?= ($task['estado'] ?? '') === 'pendiente' ? 'selected' : '' ?>>Pendiente</option>
            <option value="en_progreso" <?= ($task['estado'] ?? '') === 'en_progreso' ? 'selected' : '' ?>>En Progreso</option>
            <option value="completada" <?= ($task['estado'] ?? '') === 'completada' ? 'selected' : '' ?>>Completada</option>
            <option value="cancelada" <?= ($task['estado'] ?? '') === 'cancelada' ? 'selected' : '' ?>>Cancelada</option>
        </select>
    </div>
    <div class="flex gap-4 justify-end mt-8 flex-wrap">
        <button type="submit"
            class="bg-[#5aac44] text-white border-none py-3 px-5 rounded-lg cursor-pointer font-semibold text-[0.9rem] transition-all duration-200 shadow min-w-[100px] text-center inline-block hover:bg-[#519839]">
            Actualizar
        </button>
        <button type="button" onclick="window.location.href='<?= htmlspecialchars($cancelUrl ?? '', ENT_QUOTES, 'UTF-8') ?>'"
            class="bg-white text-[#172b4d] border-2 border-[#e1e5e9] py-3 px-5 rounded-lg text-[0.9rem] font-semibold transition-all duration-200 cursor-pointer inline-block text-center min-w-[100px] hover:bg-[#f8f9fa] hover:border-[#c1c7cd]">
            Cancelar
        </button>
        <?php if (!empty($showDelete) && !empty($deleteUrl)): ?>
            <a href="<?= htmlspecialchars($deleteUrl, ENT_QUOTES, 'UTF-8') ?>"
                class="bg-[#dc3545] text-white border-2 border-[#dc3545] py-3 px-5 rounded-lg text-[0.9rem] font-semibold transition-all duration-200 cursor-pointer inline-block text-center min-w-[100px] hover:bg-[#dc3545] hover:text-white hover:border-[#dc3545]">
                Eliminar
            </a>
        <?php endif; ?>
    </div>
</form>