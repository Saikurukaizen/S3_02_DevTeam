<?php
declare(strict_types=1);

if (empty($task) || !is_array($task)) {
    echo "<div class='flex flex-col items-center justify-center mt-12 p-8 bg-white/80 rounded-xl shadow-lg'>";
    echo "<div class='text-4xl mb-2'>❌</div>";
    echo "<h3 class='text-xl font-bold text-primary mb-2'>Tarea no encontrada</h3>";
    echo "<p class='text-gray-700 mb-4'>La tarea solicitada no existe o no se pudo cargar.</p>";
    echo "<a href='" . Environment::url('/') . "' class='text-blue-600 hover:text-blue-800'>← Volver al Kanban</a>";
    echo "</div>";
    return;
}

$estadoColors = [
    'pendiente' => '#f59e0b',
    'en_progreso' => '#3b82f6',
    'completada' => '#10b981'
];

$estadoColor = $estadoColors[$task['estado']] ?? '#6b7280';
?>

<div class="w-full max-w-4xl mx-auto">
    <h1 class="text-2xl md:text-3xl font-bold text-white mb-8 text-center">📝 Editar Tarea</h1>
    <?php
        $this->action = Environment::url('task/update?id=' . $this->task['id']);
        $this->buttonText = '💾 Actualizar Tarea';
        $this->isReadOnly = false;
        $this->showDeleteButton = true;

        include(ROOT_PATH . '/app/views/scripts/task/_form.phtml');
    ?>
</div>

<script>
    function confirmarDelete(taskId) {
        if (confirm('¿Estás seguro de que quieres eliminar esta tarea?\n\nEsta acción no se puede deshacer.')) {
            const form = document.createElement('form');
            form.method = 'POST';
            form.action = '<?= Environment::url("task/delete") ?>';
            
            const input = document.createElement('input');
            input.type = 'hidden';
            input.name = 'id';
            input.value = taskId;
            
            form.appendChild(input);
            document.body.appendChild(form);
            form.submit();
        }
    }
</script>